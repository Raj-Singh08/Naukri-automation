{
  "name": "job-profile-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Automatically keeps your Naukri profile fresh (summary + resume upload) so recruiters see you as an active candidate.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
